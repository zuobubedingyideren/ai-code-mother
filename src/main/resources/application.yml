spring:
  application:
    name: ai-code-mother
server:
  port: 8123
  servlet:
      context-path: /api
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.px.aicodemother.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn
